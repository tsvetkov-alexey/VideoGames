import React from 'react';

const NotWorking = () => {
    return (
        <div id="work-in-progress">
            <h1>Похоже идут работы</h1>
            <img src="../assets/work-in-progress.png" alt="work" />
        </div>
    );
};

export default NotWorking;
